query RunInput {
  cart {
    lines {
      quantity
      id
      merchandise {
        ... on ProductVariant {
          product {  
          metafield(key: "productDiscount",namespace: "custom"){
            value
          }
          }
        }
      }
    }
  }
}
